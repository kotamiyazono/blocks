# プロジェクト指示

## 製品方針

実装前に `README.md` を正本として読む。

- ビルド工程を持ち込まない。素の ES モジュールのまま、ブラウザが直接読める形を保つ
- ルールは `public/js/rules.js` の一箇所だけ。ブラウザとサーバが同じものを読む(二重定義を作らない)
- 着手はサーバ側でルールを一から検証する。クライアントを信用しない
- 勝敗・棋譜・プレイヤー情報を保存しない。対局中の盤面だけを置き、終わったら消す
- ひとことは「いま言っていること」だけ。会話の履歴を持たない
- 置ける場所を先回りして提示しない。考えずに確定できる状態を作らない
- 選ばせなくて済むものは選ばせない(色は席で決まる)
- 残りマスの数字は対局中に出さない。手元のカルーセルそのものが「残り」
- コードは短く保ち、既存実装・標準ライブラリ・platform機能を優先する

## 後方互換性は作らない(所有者が明示的に指定するまで)

このアプリは開発中で、利用者は所有者のみ。以下を**一切作らない・残さない**:

- 旧version保存データの読み取り移行(migration)・変換層
- 互換のためのフォールバック読み取り・併存期間
- 旧wire形式・旧schemaへの対応コード

部屋のデータは元から短命(対局中 2 時間 / 終局後 5 分)なので、形状を変えるときは
新しい形をそのまま正とする。形状に合わない既存の部屋は消えてよい。version番号の付け替えも不要。

このルールは所有者が「互換対応を作れ」と明示的に指定したときだけ解除される。

## オーケストレーション体制

メインセッション(Claude)はオーケストレーターに徹し、コード自体は書かない:

- **設計**: `.claude/agents/architect.md`(Opus5)にAgentツールで委譲
- **実装・テスト**: Codexプラグインに委譲(Agentツールの `codex:codex-rescue`、モデルは `.codex/config.toml` の `gpt-5.6-luna`)
- **レビュー**: `.claude/agents/reviewer.md`(Opus5)にAgentツールで委譲
- **E2E**: `.claude/agents/e2e-tester.md`(Sonnet)にAgentツールで委譲。実ブラウザでの確認が要るときだけ

フロー: 設計 → 実装+テスト → レビュー → 指摘があればCodexに差し戻し → 完了報告。
オーケストレーターは受け渡し・判断・報告のみ行う。軽微なtypo修正等でもこの分業を崩さない。

## テスト

```bash
npm test            # ルールの検証(通信なし)
npm run test:online # 動いているサーバに対する通し検証
```

`test:online` は既定で本番に向く。別環境は `BLOCKS_URL=http://localhost:8787 npm run test:online`。
