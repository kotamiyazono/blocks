/**
 * BLOCKS — 席の色の呼び名
 *
 * 実際の色はスタイル側の --p1 〜 --p4 に置いてある。
 * ここが持つのは「4 人戦で誰の番かを言葉にするための名前」だけ。
 *
 * サーバ側もこのファイルを読む可能性があるので、DOM には触らない。
 */

const SEAT_NAMES = ['むらさき', 'オレンジ', 'ブルー', 'グリーン'];

/** 席番号 → 色の名前。 */
export const colorName = (seat) => SEAT_NAMES[seat - 1] || '';
